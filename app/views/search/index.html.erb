<% if @location == @location.to_i.to_s %>
  <h1> Today's weather in <%= @location.to_region(city: true) %>: </h1>
<% else %>
  <h1> Today's weather in <%= @location %>: </h1>
<% end %>
  <ul>
    <li> High of <%= @high_temp %>°F </li>
    <li> Low of <%= @low_temp %>°F </li>
    <% if @condition == true %>
      <li> Rain is in the forecast </li>
    <% end %>
  </ul>

<h1> Here's your outfit:</h1><br>

<%= image_tag @outfit.image_upload.url(:medium)%><br><br>

<p> Not your location? Search for a different one: </p>
<%= render 'search_form' %>


