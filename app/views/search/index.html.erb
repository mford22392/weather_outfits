<!DOCTYPE html>
<html lang="en">

<div class="container-fluid">

  <div class="col-md-4"></div>

    <div class="col-md-4">

        <div class="panel panel-primary">
          <div class="panel-heading" align="center">

            <% if @location == @location.to_i.to_s %>
              <h3 class="panel-title">Today's weather in <%= @location.to_region(city: true) %></h3>
            <% else %>
              <h3 class="panel-title">Today's weather in <%= @location %></h3>
            <% end %>
          </div>
          <div class="panel-body">

            <table class="table table-striped table-hover ">
            <thead>
            <tr>

            <tr>
            <td align="center"> High of <%= @high_temp %>°F </td>
            <td align="center"> Low of <%= @low_temp %>°F </td>
            </tr>

            </tr>
            </thead>
            <tbody>
            </table>

          </div>
        </div>

            <% if @condition == true %>
              <div class="alert alert-dismissible alert-danger">
                <button type="button" class="close" data-dismiss="alert">close</button>
                <strong>Bring an umbrella!</strong> Rain is in the forecast
              </div>
            <% end %>

            <% if @condition == true && @condition == "squall" %>
              <div class="alert alert-dismissible alert-warning">
                <button type="button" class="close" data-dismiss="alert">&close;</button>
                <h4>Warning!</h4>
                <p>Watch out for sudden squalls!</p>
              </div>
            <% end %>

          <div class="panel panel-default">
          <div class="panel-body" align="center">
            <%= image_tag @outfit.image_upload.url(:medium)%>
          </div>
          </div>

          <div class="well well-sm" align="center">
          Not your location? Search Again
          </div>


    </div>
  <div class="col-md-4"></div>
</div>

        <%= render 'search_form' %>

        <ul>
          <% @tweets.each do |tweet| %>
            <li> <%= tweet.html_safe %></li><br>
          <% end %>
        </ul>



</html>






